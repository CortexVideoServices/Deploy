Deploy tools, docker and docker-compose files
---------------------------------------------

Useful commands

    docker-compose up -d --build
    docker-compose logs -f janus-gateway
    docker-compose down